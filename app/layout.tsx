import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Bhupinder Singh Sahmey | Software Developer",
  description:
    "Portfolio of Bhupinder Singh Sahmey — Python backend developer building scalable REST APIs, multi-cloud systems, and data pipelines.",
  openGraph: {
    title: "Bhupinder Singh Sahmey | Software Developer",
    description:
      "Backend engineer specializing in Flask, AWS, Redis, and enterprise cloud reporting systems.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
